<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="empsoft.ufcg.edu.cordeus">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".views.MainActivity"
            android:screenOrientation="landscape"
            android:theme="@style/AppThemeDefault" />
        <activity
            android:name=".views.VideoActivity"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />
        <activity
            android:name=".views.RegisterActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/AppThemeRegister"/>
        <activity
            android:name=".views.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppThemeDefault">
        </activity>
        <activity
            android:name=".views.NewCordelActivity"
            android:label="@string/title_activity_new_cordel"
            android:screenOrientation="landscape"
            android:theme="@style/AppThemeDefault"></activity>
        <activity
            android:name=".views.SplashActivity"
            android:theme="@style/AppThemeDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>